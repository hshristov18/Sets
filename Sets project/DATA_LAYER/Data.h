#include <iostream>
#include "Data.cpp"

using namespace std;

void input(arr, N);
int merge(arr1, m, arr2, n, *mergedArray);
void outputMerge();
int section(arr1, m, arr2, n, *sectArray);
void outputSecton();
